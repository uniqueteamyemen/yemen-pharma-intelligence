# نتائج اختبار صفحة العروض

تم اختبار `drugs.search` محلياً ببادئة عربية؛ أعاد endpoint حالة `200 OK` ونوع محتوى `application/json` مع نتيجة أموكسيسيلين. كما أعاد `offers.list` حالة `200 OK` ونوع محتوى `application/json` وقائمة عروض نشطة، منها عرض كتالوج باستخدام `drugId=120008`.

تم اختبار `offers.create` دون مصادقة؛ أعاد tRPC خطأ `401 UNAUTHORIZED` بصيغة JSON، وليس HTML، ما يؤكد سلامة مسار API حتى عند رفض المصادقة. ومن رابط المعاينة الخارجي أيضاً، أعاد كل من `drugs.search` و`offers.list` حالة `200` و`application/json` وكان نص الاستجابة بصيغة JSON وليس `<!doctype ...>`.

كما تم تنفيذ طلب `offers.create` من رابط المعاينة الخارجي دون جلسة؛ أعاد `401` مع `content-type: application/json` وبداية استجابة tRPC المنظمة `[{"error":...}]`، وليس HTML. بذلك تم التحقق من أن مسارات الكتالوج والقائمة والإنشاء لا تسرب صفحة SPA بديلة عند نجاح الطلب أو فشل المصادقة.

جلسة متصفح الاختبار الحالية غير مسجلة الدخول، لذلك لا يمكن تنفيذ mutation حقيقية من الواجهة في تلك الجلسة دون إنشاء جلسة مستخدم. لا تم إجراء أي تغيير لمحرك المطابقة أو مخطط قاعدة البيانات أو نموذج البيانات خلال الاختبار.
