# ملاحظات تنفيذ الهوية في المصادقة والتحميل

## حدود المصادقة

تعتمد المنصة مصادقة Manus OAuth الحالية. لا توجد صفحة تسجيل محلية أو جمع كلمات مرور، لذلك لا يُنشأ نموذج تسجيل جديد. سيُحسَّن فقط غلاف الوصول غير الموثق ورسالة الدخول، مع بقاء زر الدخول يستدعي `startLogin()` وتبقى عملية إنشاء جهة تشغيلية ضمن المسار الحالي بعد الدخول.

## مواضع التطبيق

| الموضع | التحديث المطلوب |
|---|---|
| `DashboardLayout` | تحويل حالة عدم وجود مستخدم إلى بطاقة هوية بصرية واضحة تتضمن العلامة وزر الدخول الحالي |
| `DashboardRouter` | توحيد حالة الدخول غير الموثق مع المكوّن نفسه، بدلاً من زر منفصل غير مهيأ |
| `App` | وضع طبقة تحميل انتقالية خفيفة فوق المسارات، وتبقى محتوية داخل التفضيل `prefers-reduced-motion` |
| `DashboardLayout` sidebar | استخدام علامة مصغرة مناسبة بدلاً من تصغير الرمز التفصيلي الكامل |

## معيار التحميل

لا تحاكي شاشة التحميل تقدماً وهمياً ولا تمنع التنقل. تظهر فقط خلال انتقال مرئي قصير، وتكتفي بالرمز المصغر مع إشارة نبض خفيفة، وتتوقف الحركة لمن يفضل تقليل الحركة.

## تحقق التنقل

تم التحقق بصرياً من انتقال فعلي بين `/` و`/about`: ظهرت بطاقة **جارٍ تحضير الصفحة / Preparing page** مع الرمز المصغر فوق خلفية مموهة، ثم يزيل المؤقت الإنتاجي الطبقة بعد 260 مللي ثانية. حركة النبض محصورة في `prefers-reduced-motion: no-preference`، لذلك لا تضاف حركة لمن يختار تقليل الحركة.
